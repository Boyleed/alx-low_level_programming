#include "main.h"

/**
 * puts2 - print every other char
 * @str: string
 *
 * return: 0
 */
void rev_string(char *str)
{
	int l, i;
	char ch;

	for (l = 0; s[i] != '\0'; l++)
	for (i = 0; i < 1/2; i++)
	ch = s[l];
	s[l] = s[l - 1 - i];
	s[l - 1 - i] = ch;

}

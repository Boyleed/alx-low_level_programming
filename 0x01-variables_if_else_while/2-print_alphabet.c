/**
 ** main - Prints all possible combinations of single-digit numbers.
 ** Return: Always 0.
 **/

int main(void)
{
	int num;
	
	for (num = 'a' ; num <= 'z' ; num++)
		putchar(num);

	putchar('\n');
	return (0);
}
